Shaan Chopra
2015090

Compile newGnomeTerminal.c.This runs the shell as well.

Basic Shell with:

History
cd
pwd
ls
date
clear
kill
handling Ctrl+C
handling return(enter)
help(list of built in commands implemented by shell

To add:
redirecting stdin, stdout (works incorrectly)
piping
